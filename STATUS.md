# Status

Current state and next actions. Single source for "where we are"; keep aligned with [ROADMAP.md](ROADMAP.md).

**Phase:** 01 — Implementation (gate lifted)

**State:** Phase 00 bootstrap complete. **@restormel/keys v0.1.0** is published to npm; first publish and Phase 1 manual steps are done. Phase 2 complete (Svelte, elements, React, CLI, Next.js demo, SOPHIA runbook, a11y, publish, SvelteKit demo). **Auth model (v1):** **Gateway Key (`rk_...`)** for all programmatic/backend access (Resolve, policy evaluate, routes/steps); **Session (GitHub login)** for dashboard admin/config. **Management Key** is deferred/internal-only and not required for v1 flows. **Single app:** All product surfaces live in **apps/dashboard** (SvelteKit 2 + Svelte 5): Keys landing (`/keys`), Pricing (`/keys/pricing`), Docs (`/keys/docs`) including the integration walkthrough (Phase 0–6, migration paths, verification) with optional **agent prompts** per phase (collapsed by default; `RESTORMEL_DOCS_AGENT_PROMPTS=false` hides site-wide). Dashboard at `/keys/dashboard` (Neon Auth, Neon Postgres, Gateway keys, projects, integrations, models, routes, policies, etc.). **Layout:** One header (SiteHeader) and footer (SiteFooter) across the site; GitHub link in footer only; max-width container `--rm-container-max` (72rem) site-wide; docs and dashboard side navs are **collapsible** (state in localStorage). Optional **`RESTORMEL_DASHBOARD_UI_HIDDEN`** (comma-separated section tokens) hides matching dashboard **UI** areas (nav, shortcuts, project switcher when `projects` is listed); **dashboard REST APIs unchanged** — see `apps/dashboard/README.md`. `apps/site` is **archived** (Astro/Starlight no longer used); deploy from `apps/dashboard` only. Keys experience unification (Phase A–D) done: logged-out UX and SSO, brand shell and logo, journey fixes, docs/Zuplo same-link and documentation strategy, shared tokens (`@restormel/keys-tokens`), UX contracts, reintegration seams in ARCHITECTURE.md.

**Integrations:** **Restormel Integrations** developer enablement layer added. Marketing landing at `/integrations` (top-level, extraction-friendly). Dashboard "Developer Tools" section at `/keys/dashboard/dev-tools` (CLI, MCP, AAIF tabs). New packages: `@restormel/aaif` (types + validation + runtime helper `executeAAIFRequest`), `@restormel/mcp` (tool schemas + **stdio MCP server** `restormel-mcp`, `createRestormelMcpServer()`). CLI extended: `keys models list`, `keys routing explain`, `keys catalog fetch` (public catalog HTTP check). Partner integration guide: `/keys/docs/guides/canonical-catalog`. Existing dashboard "Integrations" relabelled to "Provider Access". Usage path selector added to dashboard overview. Docs: `/keys/docs/integrations/` (overview, CLI, MCP, AAIF). Full spec: [docs/integrations/INTEGRATIONS-FULL-SPEC.md](docs/integrations/INTEGRATIONS-FULL-SPEC.md).

**Operator control-plane uplift (packs 1–8):** Added provider trust metadata (`providers/health`), route coverage summary (`route-coverage`), preflight advisory (`readiness`), route recommendation surface (`routes/{routeId}/recommend`), resolve/simulate metadata normalization (`contractVersion`, `traceId`, `decisionMetadata`), policy lifecycle parity (`/api/policies/{id}/history|publish|rollback|diff`), and provenance fields (`updated_via/updated_by/change_summary/content_hash`) for route/policy changes.

**Resolve discovery (hosts / SOPHIA):** `POST .../resolve` contract `2026-03-25` adds `route` metadata, stable `error` codes with aligned HTTP statuses, published-route gating, and metadata selection (dedicated `workload`+`stage` then shared ingestion). `GET .../routes` includes `isPublished`; `POST .../routes/{routeId}/validate-binding` for preflight. Canonical narrative: [docs/guides/resolve-to-execution-contract.md](docs/guides/resolve-to-execution-contract.md); spec: [docs/api/openapi.yaml](docs/api/openapi.yaml).

**Dogfooding semantics/doc uplift:** Added canonical guides for route category choice and resolve-to-execution boundary (`docs/guides/choosing-route-model.md`, `docs/guides/resolve-to-execution-contract.md`), plus Gateway-Key policy discovery paths for real policy lifecycle validation.

**Launch-readiness uplift (P0–P3 in progress):** Fixed high-visibility blockers (analytics hardening, broken onboarding copy, lifecycle nav removal, dashboard title coverage), shipped baseline operations UX (logs filters/status badges/no_route guidance, key-creation copy-focused flow, policy type guidance, model filter fixes), and added archetype routing surfaces (`/keys` intent cards, `/keys/docs` journey picker, `/keys/docs/how-it-fits-together`, `/keys/docs/journeys/*`, `/keys/docs/search`).

**Next:** Phase 3.2 (Keys landing page), 3.3 (Pricing), 3.4 (dashboard), etc. per [docs/reference/09-prompt-pack-phase-3.md](docs/reference/09-prompt-pack-phase-3.md). For a practical remaining backlog (completed vs partial vs missing, tech debt, next tasks), see [docs/reference/remaining-backlog-after-implementation.md](docs/reference/remaining-backlog-after-implementation.md). **SOPHIA dogfooding** (ingestion pipeline + fallback routes/policies + UI embedding for model selection): [docs/reference/sophia-dogfooding-plan.md](docs/reference/sophia-dogfooding-plan.md).

**Dogfooding / npm:** **validate** and **keys-cli** publish via `pnpm publish`. Free tier **2 projects**. **Founding Pro:** first **50** signups → **12 months Pro** ([docs/reference/founding-pro-promo.md](docs/reference/founding-pro-promo.md)). [docs/reference/npm-packages.md](docs/reference/npm-packages.md). **SOPHIA Phase 5A complete:** core integration through Phase 5 working with real KeyManager (from local tarball). Dogfood findings in [docs/reference/sophia-dogfood-findings.md](docs/reference/sophia-dogfood-findings.md). **Restormel-first assessment** ([docs/reference/restormel-first-assessment.md](docs/reference/restormel-first-assessment.md)): strengthen Restormel’s integration contract (typings, component behavior, model filtering, diagnostics) before over-fixing host app; then simplify Sophia and reassess what is truly app-specific.

**Blockers / truth source:** package availability is tracked via `npm view` checks in [docs/reference/npm-packages.md](docs/reference/npm-packages.md), not inferred from roadmap phase labels. If a package is unavailable, use the documented local/package fallback paths until publish is confirmed.

---

*Update when state or next actions change. Use roadmap-status-sync skill.*
