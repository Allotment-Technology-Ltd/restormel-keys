{
  "name": "restormel-keys-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Zuplo gateway config for Restormel Keys API (config-as-code)",
  "scripts": {
    "deploy": "zuplo deploy"
  },
  "devDependencies": {
    "zuplo": "^6.67.25"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "fastify": ">=5.7.3",
      "undici": ">=6.24.0",
      "minimatch": ">=10.2.3",
      "js-yaml": ">=4.1.1",
      "tmp": ">=0.2.4"
    }
  }
}
