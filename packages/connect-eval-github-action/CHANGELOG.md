# @restormel/connect-eval-github-action

## 0.1.0

- Initial release (verified-context roadmap, Stage 2.3): composite action wrapping
  `keys connect eval --baseline` — remote (gateway key) and local counts modes, sticky
  marker-keyed PR comment with the regression diff table, step summary, warn mode, and
  the 0/1/2/3 exit-code contract. Works on GitHub Actions and Forgejo (GitHub-compatible
  issues API).
