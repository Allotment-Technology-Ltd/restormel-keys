# Changelog

All notable changes to `@restormel/state` are documented here.

## 0.1.0

- Initial publish: `StateEvent` union, `MemoryPolicy`, `projectWorkingMemory`, prompt/debug helpers, Stoa-oriented event factories, context-pack + observability correlation helpers.
