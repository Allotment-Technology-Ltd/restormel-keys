# Rename to .env.example after clone if your tooling expects that filename.
# Copy to .env for local dev. Use obvious placeholders only — never commit real secrets.
# PUBLIC_SITE_URL=https://restormel.dev
# DATABASE_URL=postgresql://USER:PASSWORD@HOST.example/DBNAME?sslmode=require
